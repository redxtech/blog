# ./blog.sh < /dev/null
> a place for me to share interesting linux & dev stuff

Not too much info here. I'll update this as I add more stuff to it.

## Usage
This blog uses VitePress, so once you clone the repo and install the
dependencies, `yarn dev` will bring up the dev server. `yarn build && yarn serve`
will make a static version of site for deploying.
